{
  "name": "nginx-oidc-docker",
  "repository": "https://github.com/navikt/nginx-oidc-docker.git",
  "author": "NAV",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "yarn up; mocha",
    "mocha": "mocha",
    "build": "docker build -t navikt/nginx-oidc .",
    "down": "docker-compose down",
    "up": "docker-compose up --build -d"
  },
  "dependencies": {
    "dotenv": "^6.2.0",
    "mocha": "^6.0.2",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "wait-on": "^3.2.0"
  }
}
