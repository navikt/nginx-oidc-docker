{
  "name": "nginx-spa-docker",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/navikt/nginx-spa-docker.git",
  "author": "NAV",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "yarn up; mocha",
    "build": "docker build -t navikt/nginx-spa-docker . --build-arg HTTP_PROXY=$HTTP_PROXY --build-arg HTTPS_PROXY=$HTTP_PROXY",
    "down": "docker-compose down",
    "up": "docker-compose up --build -d"
  },
  "dependencies": {
    "dotenv": "^6.2.0",
    "mocha": "^6.0.2",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "wait-on": "^3.2.0"
  }
}
